# WageSlave System — závislosti
# Instalace: pip install -r requirements.txt

# UI & ikony
pillow>=10.0.0

# Systémový tray
pystray>=0.19.0

# Windows API (event log, dark title bar)
pywin32>=306

# SVG renderer (bez systémových závislostí, čistý Rust binding)
resvg-py>=0.2.0

# Build nástroj (pouze pro kompilaci do .exe, není potřeba pro spuštění skriptu)
pyinstaller>=6.0.0
